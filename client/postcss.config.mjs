const config = {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // Autoprefixer is a PostCSS plugin that automatically adds the correct vendor prefixes to your CSS, so it works consistently across different browsers. for example on firefox, old browsers ...
    },
};

export default config;
